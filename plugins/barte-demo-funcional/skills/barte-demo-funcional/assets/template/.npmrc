# O barte-design-system declara peer react ^18 e o app roda React 19. O DS
# funciona no 19 — o que falha é a resolução estrita do npm. Mesma linha que o
# frontend do barte-copilot carrega, e pela mesma razão.
legacy-peer-deps=true
